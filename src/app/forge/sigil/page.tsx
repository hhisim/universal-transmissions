import PortalComingSoon from "@/components/experience/PortalComingSoon";

export default function SigilForgePage() {
  return (
    <PortalComingSoon
      eyebrow="[ Forge ]"
      title="Sigil Forge"
      description="The Sigil Forge is not being presented as a finished public tool yet. Keep the concept visible, but lock it clearly as coming soon until the full ritual / generation flow is ready."
      accent="var(--ut-gold)"
    />
  );
}
