import PortalComingSoon from "@/components/experience/PortalComingSoon";

export default function CymaticTonoscopePage() {
  return (
    <PortalComingSoon
      eyebrow="[ Experience Portal ]"
      title="Cymatic Tonoscope"
      description="This instrument is still in development. The card stays visible so the portal feels whole, but the Tonoscope itself is not being presented as a finished live experience yet."
      accent="var(--ut-cyan)"
    />
  );
}
